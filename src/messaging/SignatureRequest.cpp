/**
 * @file SignatureRequest.cpp
 * A completely unnecessary cpp file that just declares the existence of the
 * static constant SignatureRequest::type, which was already defined in the header.
 * @date Nov 23, 2016
 * @author edward
 */

#include <adq/messaging/SignatureRequest.hpp>

const constexpr adq::messaging::MessageType adq::messaging::SignatureRequest::type;


